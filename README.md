# Physics Asset Body Tool

Editor-only Unreal Engine plugin that adds a dockable Slate window for direct `UPhysicsAsset` body and constraint authoring. The implementation edits standard physics asset data (`SkeletalBodySetups`, `AggGeom` primitives, and `ConstraintSetup`) and uses transactional editor operations for undo/redo.
